import styles from "@/common/HeroDetails/HeroDetails.module.css";

const HeroDetails = () => {

  return (
    <section className={styles.hero}>
      <div className={styles.heroContainer}>
        hero
      </div>
    </section>
  );
};

export default HeroDetails;