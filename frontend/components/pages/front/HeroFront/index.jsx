import styles from "@/pages/front/HeroFront/HeroFront.module.css";

const HeroFront = () => {

  return (
    <section className={styles.hero}>
      <div className={styles.heroContainer}>
        hero
      </div>
    </section>
  );
};

export default HeroFront;